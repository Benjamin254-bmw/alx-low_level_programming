#include "main.h"
/**
 *add - addition operation
 *
 *@i: the number t0 be added to k
 *@k: the number to be added to i
 *
 * Return: sum of the two numbers
 */

int add(int i, int k)
{
	return (i + k);
}
